<div class="card">
  <div class="card-header">
    {{ question.title }}
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p>{{ question.description }} </p>
    </blockquote>
  </div>

  {{> answer/create }}

  {{#each answers}}

    {{> answer/list this}}

  {{/each}}
</div>